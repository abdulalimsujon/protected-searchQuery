// import { useState } from "react";

// import React from 'react';

// const Login = () => {
//     const [logged, setLogged] = useState(false);
//     return <div>

//         {
//             logged ? <button onClick={() => setLogged(!logged)}>Login</button > : <button onClick={() => setLogged(!logged)}>LogOut</button>
//         }


//     </div>

// };

// export default [logged];

